export function backupPostgres(url: string) {
  console.log(`[POSTGRES] Would back up from: ${url}`)
}

export function backupMySQL(url: string) {
  console.log(`[MYSQL] Would back up from: ${url}`)
}

export function backupMongo(url: string) {
  console.log(`[MONGODB] Would back up from: ${url}`)
}

export function backupSQLite(url: string) {
  console.log(`[SQLITE] Would back up from: ${url}`)
}
